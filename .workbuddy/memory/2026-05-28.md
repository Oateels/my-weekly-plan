# 2026-05-28

- 修复周计划模板自媒体模块的视频发布状态持久化：新增 `wb-${WEEK_KEY}-videos` localStorage 读写，点击"已发布/待发布"后立即保存并刷新统计；页面初始化时先恢复视频状态再更新统计。同步修改 `index.html` 和 `周计划模板.html`，并将 service worker 缓存版本升级到 `weekly-planner-v10`。提交并推送至 GitHub：`cfee8a3 Fix media video publish state persistence`。
- 修复【生活管理 → 健身记录】填写后刷新内容丢失：给 `exe-dur-input`（分钟数）和 `exe-note-input`（记录备注）同时绑定 `oninput` 事件，确保实时触发 `saveExeData()`。缓存版本升级到 `weekly-planner-v11`。提交：`0943463`。
- 深度修复健身记录数据持久化：1) 在 `saveExeData` 加守卫（`rows.length === 0` 时直接返回），防止页面初始化异步竞态时用空 DOM 覆盖已有 localStorage 数据；2) 在 `loadExeData` 完成后通过事件委托在 `#exe-sessions` 容器上绑定 `input/change` 事件（双重保险），同步总时长显示 `updateExeStats()`。缓存版本升级到 `weekly-planner-v12`。提交并推送：`f63a92b Fix fitness record: guard empty DOM save + event delegation`。
- 修复【今日资讯】刷新按钮无效问题：原因是 rsshub.app 在国内无法访问，导致每次刷新都失败并退回旧数据。新方案：① 点击刷新立即用 Fisher-Yates 算法对本地 NEWS_SEED 池随机洗牌展示（保证每次有变化）；② 静态种子数据从16条扩充到52条（每分类6-8条）；③ 删除 parseRssHub 函数，改用 allorigins 代理尝试抓取 36kr/少数派/虎嗅 RSS（后台静默，成功才追加到顶部）。缓存版本升级到 `weekly-planner-v13`。提交并推送：`db993ac Fix news refresh: local shuffle + expand seed data + drop rsshub`。
