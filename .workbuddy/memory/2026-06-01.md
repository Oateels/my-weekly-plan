# 2026-06-01

- 实现跨周数据回溯——周切换导航：
  - 问题：WEEK_KEY 按当天日期计算，新周一打开只认本周键名，上周数据虽存于 localStorage 但 UI 无入口查看
  - 方案：新增 `DISPLAY_WEEK` 变量 + 导航机制
  - 所有读操作（loadXxx）改用 DISPLAY_WEEK；所有写操作（saveXxx）保留 WEEK_KEY
  - 天气栏添加「<」「>」按钮，点击自动保存当前表单 → 切换目标周 → 重新加载
  - 查看历史周时：📋 标记、禁用新增按钮、显示「查看历史」提示、opacity 微暗
  - 点击「📅」按钮一键回到本周
  - 新增 offsetWeekKey() 处理跨年回绕
  - 新增 getExeWeekKeyForDisplay() 处理健身模块日期计算
  - getVideoKey(weekKey) 参数化，getExeData(forDisplay) 参数化
  - 健身记录改用 getExeWeekKeyForDisplay 加载历史周数据
  - 涉及模块：任务、视频、睡眠、健身、会议、目标、灵感、自媒体点评、粉丝、日记、播客、天气快照
- SW 缓存版本升级：v15 → v16
- Git commit `b3248c1` 已推送至 origin/main
